---
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
#     Please read more about how to change this file at
#     https://www.github.com/GoogleCloudPlatform/magic-modules
#
# ----------------------------------------------------------------------------
# Pre-test setup
- name: delete a service account
  gcp_iam_service_account:
      name: "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
      display_name: "Ansible IAM Service Account Module"
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - https://www.googleapis.com/auth/iam
      state: absent
#----------------------------------------------------------
- name: create a service account
  gcp_iam_service_account:
      name: "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
      display_name: "Ansible IAM Service Account Module"
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - https://www.googleapis.com/auth/iam
      state: present
  register: result
- name: assert changed is true
  assert:
    that:
      - result.changed == true
- name: verify that service_account was created
  shell: |
    gcloud iam service-accounts  --project "{{ gcp_project }}"  describe "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
  register: results
- name: verify that command succeeded
  assert:
    that:
      - results.rc == 0
# ----------------------------------------------------------------------------
- name: create a service account that already exists
  gcp_iam_service_account:
      name: "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
      display_name: "Ansible IAM Service Account Module"
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - https://www.googleapis.com/auth/iam
      state: present
  register: result
- name: assert changed is false
  assert:
    that:
      - result.changed == false
#----------------------------------------------------------
- name: delete a service account
  gcp_iam_service_account:
      name: "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
      display_name: "Ansible IAM Service Account Module"
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - https://www.googleapis.com/auth/iam
      state: absent
  register: result
- name: assert changed is true
  assert:
    that:
      - result.changed == true
- name: verify that service_account was deleted
  shell: |
    gcloud iam service-accounts  --project "{{ gcp_project }}"  describe "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
  register: results
  failed_when: results.rc == 0
- name: verify that command succeeded
  assert:
    that:
      - results.rc == 1
      - "\"PERMISSION_DENIED: Permission iam.serviceAccounts.get is required to perform this operation on service account\" in results.stderr"
# ----------------------------------------------------------------------------
- name: delete a service account that does not exist
  gcp_iam_service_account:
      name: "ansible-service-account-module@{{ gcp_project }}.iam.gserviceaccount.com"
      display_name: "Ansible IAM Service Account Module"
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - https://www.googleapis.com/auth/iam
      state: absent
  register: result
- name: assert changed is false
  assert:
    that:
      - result.changed == false
